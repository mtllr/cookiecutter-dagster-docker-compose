load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes pipelines.
  - grpc_server:
      host: {{cookiecutter.project_name}}_user_code
      port: 4000
      location_name: "example_user_code"
